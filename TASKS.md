# 📋 Tasks — New Blood

**Status:** ✅ LAUNCHED — 2026-06-09 (live on prod, version-controlled on `main`). Optional polish deferred.
**Goal (one sentence):** Launch the redesigned New Blood site — new theme + reworked content — live and clean. — **Achieved.**

---

## ▶️ Doing now
- [ ] **🎯 9/2 — REPOSITION newblood.com AROUND TERRITORY-DEFENSE ENGAGEMENTS** (from the OHDBalt win): plan v2 at `docs/newblood-territory-repositioning-2026-09.md`; phase-one spec `docs/superpowers/specs/2026-09-02-territory-phase-one-design.md`. **✅ PHASE ONE LIVE 9/3. ✅ PHASE TWO LIVE 9/3** (`/four-gaps-audit/`, prod page ID 6889; see Done). **✅ 9/3 pm: homepage + services executive-scan passes LIVE** (see Done). **✅ 9/3 eve: "Why New Blood" section LIVE on /about/ (see Done).** **9/3 late: Operations Platform shape doc v1 WRITTEN** (`docs/products/operations-platform-shape-2026-09.md`, brainstormed with Jeremy: one record for the whole business; connected mode = their system stays the record (the easier sell), owned mode = we build it, years in; four pillars = record + attribution, four agents (voice last, own risk rules), portal, reporting; phased on top of a live Territory; from $90,000 build + a monthly scoped with it; multi-location dropped as a pillar, kept as a price line). **▶️ Next: Jeremy reviews the doc → then print "from $90,000" on the pricing card (section 8 has the copy).** ⚠️ Also pending Jeremy: prod About page title is still "We Are WordPress Management Specialists" and the site tagline "Wordpress Specialists" (feeds Yoast schema) → `! bash .superpowers/sdd/2026-09-02-territory-phase-one/prod-about-title.sh` (local tagline already changed to "Modern websites, expertly managed."). Then: Operations Platform shape doc before it gets a price. ⚠️ Open decision: `signal-intro` pattern is not placed on About (local or prod); its CTA now points at the audit page. Open: Tune tier + Manage checklist still say WordPress on purpose (true); generalize only if Jeremy asks.
- [ ] **New Blood Discovery form + combined report (OHDBalt edition)** — ✅ BUILT & VERIFIED & **🚀 DEPLOYED TO PROD (2026-06-19)**. Form LIVE at https://newblood.com/discovery/overhead-door (200, renders 12 service rows); admin-gated combined report LIVE at /discovery/overhead-door/report (404 logged-out / 200+renders for admin); `wp_nb_discovery_responses` table created on prod (schema v2), rewrites flushed. Deployed via new hardened **`deploy.sh`** (theme-only allowlist, true dry-run, 0 deletions — additive). Per-task + final whole-feature reviews passed; hardened (nocache, honeypot+throttle, mail logging, admin gate). **🆕 UX REFINEMENT (2026-06-22, 6 commits on `feature/redesign`, code-complete + automated-verified):** sliders → tap-friendly segmented buttons (fixes the all-zero-default + mobile problems), 12 capabilities grouped into 4 named clusters, vision-first section order, "Step N of 5" + scroll progress + skip line, and a new free-text web-leads baseline question — all on the unchanged 0–10 data model. **✅ 2026-06-22: refinement merged to `main` (PR #2, merge commit `da23571b`) + DEPLOYED TO PROD via `./deploy.sh` (11 files, 0 deletions). Live-verified at https://newblood.com/discovery/overhead-door/ (200; 30 segmented groups, 4 clusters, progress bar, leads field; old sliders gone; headline fixed; CSS cache-bust refreshed).** **✅ 2026-06-22: EMAIL SENT to Chase** (refined draft — live link + intentional multi-stakeholder ask). DB capture verified on prod (test row id 1 stored with all refined fields: segmented importance/handling, server-computed gaps, `leads_per_month`, vectors). **▶️ Now awaiting:** Chase + Paul + ops to each fill the form → submissions accrue at `/discovery/overhead-door/report` (consensus + divergence). Then build the comprehensive plan + pricing and present it — **walk-through prep ready: `docs/clients/ohdbalt-discovery-walkthrough-prep.md`**. **⚠️ Open:** prod `wp_mail` doesn't deliver (no SMTP) — install WP Mail SMTP so you're notified on each submission; until then, check the report URL manually. Draft email kept at `docs/clients/ohdbalt-discovery-email-DRAFT.md`.** _(feeds the OHDBalt comprehensive proposal)_
  - ✅ 2026-06-19 — **Combined stakeholder report built + verified** (`inc/discovery/{aggregate,report}.php` + `assets/css/discovery-report.css`): aggregation engine (pure — means/gaps/divergence/qualitative), admin-gated `/discovery/{slug}/report` (404 logged-out), ranked gap map with Team-split divergence callouts (threshold ≥4/vector ≥40), non-destructive soft-exclude (reversible toggle, Excluded roster), 5/5 unit tests PASS, multi-stakeholder render confirmed SPLIT_SHOWN/ROSTER/EXCLUDE_FORMS; reusable per-instance (no OHDBalt hardcoding).
  - 📌 **MONDAY 6/22 PICKUP** (in priority order):
    1. **Test prod SMTP** — submit a throwaway entry at https://newblood.com/discovery/overhead-door, check whether `joms@newblood.com` gets the summary email. If nothing arrives, install **WP Mail SMTP** on prod (Nexcess) and reconfigure. _DB persistence already works, so no submissions are lost in the meantime — this only gates the notification email._
    2. **Merge PR #2** (`feature/redesign` → main) so git matches what's live on prod (`gh pr view 2`). Prod runs the feature/redesign theme; main is still pre-discovery.
    3. **Send Chase the link** — draft ready at `docs/clients/ohdbalt-discovery-email-DRAFT.md`. Ask him to loop in **Paul + whoever owns ops/CRM** so each fills it individually; then view synthesized results at `/discovery/overhead-door/report` (logged into wp-admin).
    - Redeploy if needed: `./deploy.sh` (dry-runs first; theme-only, additive). Prod report shows the empty state until real stakeholders submit (the earlier test row was on the LOCAL install, not prod).
- [ ] **Jeremy: call with Chase (OHDBolt) 6/11** — discovery kit at docs/clients/seo-discovery-call-kit.md; after the call, review the Search & AI Visibility doc (docs/clients/search-ai-visibility-program.pdf) and send. Optional: publish as unlisted URL on newblood.com (awaiting Jeremy's content approval).
- [x] ✅ **Launch complete & signed off (2026-06-09)** — site live on prod, merged to `main`, commerce intact, SEO meta + /case-studies/ 301 done.
- [ ] _Deferred (optional polish, no rush):_ mobile/cross-browser pass · test a WooCommerce checkout · confirm footer email/phone are current.
- [ ] **Optional Nexcess ticket** (from 7/24 Signal-audit): ① nginx-level deny for `xmlrpc.php` (403 now comes from PHP; .htaccess is ignored on their nginx-only platform) · ② ask why platform bot rules 503 `facebookexternalhit` over plain HTTP (harmless volume, but it's what fetches FB link previews). Signal will confirm the 404/probe classes drain over the next 30-day window.

## 🔜 Up next
- [ ] **Signal Logs**: cron the collectors (Nexcess pull + `ingest-vercel` daily) so windows accrue hands-free · move `signal_events` off newfoodcenter's Neon into its own DB before v1 · monthly report regeneration.
- [ ] **Signal Logs v1** — multi-tenant web UI + client login + scheduled collection (cron). Jeremy decides: pricing/bundling, UI home (signal.newblood.com?), stack (note: ingest endpoint seeds whatever stack v1 uses). v0 live in `signal-logs/` (4 sites; starsvolleyball pending pubkey install).
- [ ] **🧭 New Blood 5-year brainstorm** (pinned 6/12) — block a few hours; seed notes in `docs/playbooks/trusted-client-seo-playbook.md` §"Beyond the window" (owned properties as income generators · service→product · the 3–5 yr trust window).
> _Launch checklist — done items checked off this activity._
- [x] Full backup before go-live (Nexcess, downloaded)
- [x] Cutover to production (theme activated, content migrated, commerce intact)
- [x] Post-launch smoke test on the live URL (pages 200, footer/case-study fixes, contact form, commerce untouched)
- [x] Pushed to GitHub + merged `feature/redesign` → `main`
- [ ] Proofread all reworked content (typos, headings, calls-to-action)
- [ ] Mobile + cross-browser check (Safari, Chrome, mobile widths)
- [ ] Test every link, form, and button (contact form verified; checkout still to test)
- [ ] SEO: page titles, meta descriptions, sitemap; 301s for legacy URLs (/case-studies/ → /work/)
- [ ] Analytics installed and firing

## ✅ Done (newest first)
- [x] 2026-09-03 — **🚀 "Why New Blood" section LIVE on newblood.com/about/** (pattern `newblood/why-new-blood`, appended to page 4529 on prod by Jeremy's script; marks=3 live). Brainstormed with Jeremy from the OHDBalt "Why New Blood" slide; his reframe set the shape: businesses only care if it says their problem gets solved, so three outcome-first commitments each backed by a confirmed practice (one of a few, on it weekly, direct line to the builder). No vendor comparison, no "one person start to finish" claim, no engagement number. Commit `c563bce2`, pushed.
- [x] 2026-09-03 — **🚀 Homepage + Services executive-scan passes DEPLOYED + LIVE** (11 theme files, checksums verified, transients cleared by Jeremy's script). Home: hero names the audience ("25+ years of craft" eyebrow), line marks on both doors, "Trusted by" strip removed, services-card copy cleaned, showcase trimmed to 3 + "See all six projects", **new `newblood/proof-figures` band** (0.29 s TTFB · 231 brands · 36 years, each linked to its case study) replaces the anonymous "Sarah M." testimonial (pattern kept on disk), CTA → "reply within a business day", empty Notes slot removed. Services: four AI-rendered stock illustrations replaced by mechanism diagrams (Build ingredients → one site; Tune diagnose/fix/prove + before/after weight; Manage daily loop; Empower content layer on our structure), four-step bar under "How we arrange a project", one-line intro. QA'd desktop + 375 (portrait variants, no overflow). Commits `91f21aba`, `a4b8a29e`; pushed.
- [x] 2026-09-03 — **🚀 Territory phase two DEPLOYED + LIVE: the Four Gaps Audit page.** `/four-gaps-audit/` (prod ID 6889, Yoast set): opening, three-readings→four-gaps diagram + 3 cards, four deliverables, four-step bar, $1,500–$2,500 card → `/contact/?subject=four-gaps-audit` (theme-owned lead-in + pre-filled message). Territory + pricing audit buttons repointed; "ad platform" → "whatever advertising you run". Same deploy carried the copy corrections Jeremy flagged: pricing FAQ "update it yourself" → modern code base + admin backend; "Do you build on platforms other than WordPress?" → "What do you build on?" (Next.js-first, WordPress where it fits); Services Empower block no longer claims WordPress-only block editor. 9 theme files, checksums verified; live checks all 200. Pushed.
- [x] 2026-09-03 — **🚀 Territory phase one DEPLOYED + LIVE on newblood.com.** Theme synced (14 files, checksums match), `/territory/` page created on prod (ID 6888) with Yoast title/desc, pattern transients cleared; live checks: `/` two-door section, `/territory/` (8 marks, 2 diagrams, 3 gradient sections), `/pricing/` Flagship pill, `/about/` rewritten copy — all 200. Same-day local review edits before deploy: gaps eyebrow → "Do any of the following connect?", read-only card → "Your systems stay yours", plan card lost the $1,950/$1,250 split + paid-search paragraph, and **custom executive-scan imagery** (8 SVG line marks, four-gaps loop diagram w/ portrait variant, 90-day timeline bar) QA'd at 375 + desktop. 19 commits pushed (`58bfe28d..fbeb522e`). New standing rule in `~/Herd/CLAUDE.md`: no page is content-complete without the imagery pass.
- [x] 2026-09-02 — **Territory phase-one, Task 6/6: responsive + visual QA — clean bill, no fixes needed.** Checked `/`, `/territory/`, `/pricing/`, `/about/` at 375/768/1024/1280: no horizontal overflow anywhere; header nav doesn't wrap at 1024/1280 (7 items); every `.nb-reveal` section reveals on scroll (11/11 home, 24/24 territory, 14/14 pricing, 5/5 about — verified via real scroll, not just DOM inspection); `/pricing/` door-one is 2 cards in a row and door-two 3 cards at 1280; `/territory/` gaps + commitments are 4-across at 1280 and 2×2 between 769–1024. `resize_window` proved non-functional in this environment (confirmed via repeated tests) — worked around with a same-origin iframe sized to each target width for layout checks, and real wheel-scroll on the native tab for the reveal check. `patterns.css` untouched; no commit. Report: `.superpowers/sdd/2026-09-02-territory-phase-one/task-6-report.md`. **Territory phase-one (Tasks 1–6) is complete.**
- [x] 2026-09-02 — **Territory repositioning: plan reviewed against the live site, revised to v2, phase-one design spec written + committed** (`1d6d0fcb`). Found four live price-modesty lines (hero, About ×3) and an FAQ "1-2 weeks" promise; both fixes are step 1 of phase one.
- [x] 2026-08-20 — **🐛 FOUND: Signal over-counts "Humans arriving FROM an AI assistant" in every report.** `signal.py:401` matches referrers with `if dom in ref` — a bare substring test — so a site's **own** utm-tagged URL (`lomalindamarket.com/?utm_source=chatgpt.com`) matches `chatgpt.com` and every internal click after a tagged landing gets recounted as a fresh AI referral. On lomalindamarket that turns ~16 real sessions into a reported **519** (now 557 as the window rolls). Verified against the raw events: only **3** hits carry the true external referrer `https://chatgpt.com/`; the 14 main IPs each hit 38–40 distinct pages in one day and never returned. **This matters beyond one client — it's a headline metric in a product being sold.** ⚠️ **SCOPE CORRECTED 8/20 — it is NOT signal.py-only.** The same substring match exists in **three** places, and the important one is the live product: `signal-ingest/lib/bots.ts:22` (`aiReferrer()`, `ref.includes(dom)`), `signal-ingest/lib/bots.ts:34` (`AI_REFERRER_PATTERNS`, builds `%dom%` SQL ILIKE patterns), and `signal-logs/signal.py:401`. Both repos share `signal-ingest/lib/bots.json` → `aiReferrers`. **Every tenant's dashboard on signal.newblood.com has been over-counting**, not just the local report. Worse: `app/api/cron/rollup/route.ts:105` calls `aiReferrer()` to write `refLabel` into **stored aggregates**, so historical rollup rows are wrong too and a fix alone won't correct them. **▶️ Fix:** match on the referrer's **host** (`hostname === dom || hostname.endsWith('.'+dom)`), fail closed on unparseable referrers. This **supersedes** the earlier suggestion here of comparing against the site's own host — host equality is simpler, needs no per-tenant domain, and correctly rejects any third-party URL carrying a `utm_source`. Keep Python and TS identical (bots.ts already documents that it mirrors signal.py). Do NOT touch the bot-classification path (`classify()`, ChatGPT-User/GPTBot/OAI-SearchBot) — those are UA matches, IP-verified, and correct. **✅ 8/20: FIX LANDED** in both repos — host matching (`hostname === dom || endsWith('.'+dom)`), fail-closed, plus a host+path rule for the `www.bing.com/chat` key the brief missed (a bare hostname check would have relabelled all Bing search as Copilot). Dead `AI_REFERRER_PATTERNS` removed (zero consumers). 19/19 TS tests pass; Python verified against the same 16-case table. **Measured across 3.9M local events: 3,555 → 54 AI referrals (−98.5%)** — akta 1,990→25, ohdbalt 632→13, lomalindamarket 560→6 (ChatGPT 557→3, exactly as predicted), newblood 288→4, rookgame 85→6, dadabilities 0→0. ⏸️ **Backfill of stored aggregates NOT run — awaiting Jeremy**, and only ~1.7% of it is recomputable from Neon (raw is a 7-day buffer); details + options in `signal-ingest/TASKS.md`. ⚠️ mmc is the only client-visible site and holds 293 inflated hits — dashboard only, no digests were ever sent.
- [x] 2026-08-19 — **CLOSE-OUT: Speaking Well case study live + the reveal bug fixed fleet-wide, all pushed.** 4 commits on `feature/redesign`, pushed to GitHub (`eedb1efc..f16a9494`) — the push also carried 19 older unpushed commits from prior sessions (GENNOW Eds. 1–12, speakingwell tech-doc edits), so `origin/feature/redesign` is now current with local. **Commits:** `a1badba0` publish · `e987223e` SWOG reveal fix · `48f4a438` README root-cause · `f16a9494` remaining-four sweep. **Net result:** `/case-study-speaking-well-of-god/` live with its Work-page card; **all 7 case-study files free of the wrapper `nb-reveal`**; and the README no longer propagates the bug to the next case study (step 3 used to say "template from `57cards.html`" — the very file carrying it, which is how SWOG caught it 3 months after the same bug was fixed on newfoodcenter). **▶️ Nothing outstanding on this work.** Two known, deliberate loose ends: the Work grid's odd trailing cell (leave as is, 6th case study coming) and `overhead-door.html` fixed locally but unpublished (withheld during OHD sales talks).
- [x] 2026-08-19 — **Swept the remaining four case studies for the same blank-until-scroll bug.** Dropped the wrapper `nb-reveal` from `57cards`, `mikes-master-classes`, `ca-lindman` and `overhead-door`; **all 7 case-study files are now clean.** Diffed each prod body against its local file *before* overwriting — all three live ones were byte-identical, so no prod edits were clobbered. Pushed via `wp post update` (57cards **6850**, mikes **6848**, ca-lindman **6849**), stored content verified IDENTICAL each, caches purged. `overhead-door` has **no page on prod under any status** (it's deliberately withheld during the OHD sales talks) so there was nothing to push — the local file is fixed for whenever it does go up. **Browser-verified at scroll 0:** all three render with opacity 1 and images loaded. ⚠️ **These were worse than 'latent':** at a 467px-tall window, 57cards' wrapper is 3294px (max ratio **0.14**) and mikes-master-classes' is 4230px (**0.11**) — both *below* the 0.15 threshold, meaning on a short window those pages would never have revealed at all, at any scroll position. Live sweep confirms all 6 published case studies now 200 and clean.
- [x] 2026-08-19 — **Fixed: Speaking Well case study rendered blank until you scrolled.** The whole 129-line body sat inside a single `nb-reveal` wrapper, which is `opacity:0` until the IntersectionObserver in `scroll-reveal.js` adds `.is-visible` at `threshold: 0.15`. For an element taller than the viewport the visible fraction can only ever reach `viewportHeight/elementHeight` — this body started below 15% and only crossed it once you scrolled a little, so the page loaded blank and appeared on scroll. **This is the documented long-wrapper gotcha, and it had been hit before:** `newfoodcenter.html` and `rtz-audio-visual.html` (the two most recent case studies) already omit `nb-reveal` from the wrapper — Speaking Well inherited the older template. Dropped `nb-reveal` from the outer group to match; the body is flat (no inner section groups), so per-section reveals would have meant restructuring the file. Pushed with `wp post update 6875` (not WP-Admin), stored content verified IDENTICAL, caches purged. **Browser-verified at scroll position 0:** full body + hero image render on load (img `complete:true`, naturalWidth 1440, opacity 1). Also checked the 6th card didn't break `/work/`: grid is 2234px tall vs 881px viewport → max ratio **0.394**, comfortably above the 0.15 threshold, opacity 1. Card measures **588px, identical to all four non-featured neighbours** (featured is 1200px). 📌 Cosmetic, **decided 8/19 — leave as is**: five non-featured cards in a 2-col grid leaves SWOG alone in the last row with an empty cell beside it. Jeremy is adding a 6th case study soon, which squares the grid off on its own. Don't restructure the grid to close the gap.
- [x] 2026-08-19 — **Speaking Well of God case study PUBLISHED to production.** Live at https://newblood.com/case-study-speaking-well-of-god/ (200) with its card on /work/. Three pieces, only one of which rides a normal deploy: ① theme pattern `portfolio-grid.php` deployed via `./deploy.sh` (dry-run confirmed **1 file, 0 deletions** — exactly the intended change); ② the screenshot, which is gitignored *and* outside deploy.sh's theme-only sync root, so prod had never seen it — `scp`'d + `wp media import`ed (md5 verified identical, 7 derivatives generated); ③ the page, which existed only in the local DB as post 6766 — created fresh on prod as **post 6875** from the source of truth `docs/case-studies/speaking-well-of-god.html`, stored content diffed **IDENTICAL** to the file. ⚠️ **The attachment-ID trap fired:** both the pattern and the case-study HTML hard-coded local id **6765**; prod's import assigned **6874**. Fixed in *both* files before pushing content, else the hero image and the Work-page card would both have rendered broken. (The filename did *not* collide, so the path `/wp-content/uploads/2026/08/swog-case-study.png` held — only the id moved.) Verified: page 200 · hero image **fetched** 200, `image/png`, md5 matches local byte-for-byte · single H1 (no duplicate header) · no `newblood.test` leak · card is a real child of `nb-showcase-grid` (DOM-parsed ancestor chain) carrying plain `nb-showcase-card`, **not** `nb-showcase-featured` — so it sits at neighbour width, and only `.nb-showcase-featured` takes `grid-column: 1/-1` · WP emitted a `srcset`, which independently proves id 6874 resolves on prod · all 6 case-study links 200. Nexcess Edge CDN + page cache flushed, rewrites flushed, staged files cleaned up. 📌 Note: the 2 files now carry the **prod** id 6874 while the local DB's post 6766 still references 6765 — local still *renders* fine (root-relative src), but the local block editor's image reference is stale. ✅ **Committed + pushed 8/19** (`a1badba0`) — see the close-out entry below for the full push.
- [x] 2026-08-17 — **GENNOW PRMT01 Edition No. 12 PUBLISHED — the run is complete (12 editions).** The student's corrected export landed (`…Newsletter (3).zip`, mtime 8/16 16:04, 12 images) and **fixes everything that got Ed. 12 held on 8/14**: the body is genuinely new (Waterfalls / At the Beach / Interview Time — band RMSE **11,655–28,967** vs Ed. 11 across all 8 bands, where the 8/14 draft was 57–97), and the intro now runs complete: _"Our last days in Puerto Rico were very active. On Sunday, we parted ways with Yorba Linda Church, then went to the waterfalls and a shopping center. Monday was the day we went to the airport and left Puerto Rico."_ Ed. 12 (**Day 12, Ponce, Aug 10**) live at `page-12.html`; root symlink → Ed. 12, chain now 1↔…↔12. Eds. 1–11 band-diffed against this export and **unchanged** (worst band 2,264 vs the ~2,100 floor) so pages 1–10 untouched; page 11 patched only (+next pill, −canonical → page 12). Verified: root + page-1 + page-11 + page-12 all 200 with matching Content-Length, full SHA-256 match on the two changed pages, dir 755, chain walked live in-browser (root → Ed. 12 → Ed. 11 shows both pills). Committed `b028b64b`. 📌 The artwork itself signs off — the credits block reads as a final edition ("I'm sorry the last edition came out so late"), so expect no Ed. 13 unless the student says otherwise.
- [x] 2026-08-09 — **GENNOW PRMT01 Edition No. 11 PUBLISHED** — Ed. 11 (**Day 11, Ponce, Aug 8**) live at `page-11.html`; root symlink → Ed. 11, chain now 1↔…↔11. Eds. 1–10 unchanged (worst band 2265 vs ~2100 floor) so pages 1–9 untouched; page 10 patched only (+next pill, −canonical → page 11). 13 URLs 200, all 11 sizes match local, full SHA match on the two changed pages + root, dir 755, 11-hop chain walked live. Committed `035448e3`. 📌 **Zip filename suffixes are NOT chronological** — this arrived as `…Newsletter (1).zip` (an old slot reused after the Downloads cleanup). **Always check the zip's mtime + image count, never trust the `(N)` suffix.** Verification sweep now uses `curl -sI` + Content-Length for all pages and full SHA only on changed ones — fetching every body was timing out at ~30 MB.
- [x] 2026-08-08 — **GENNOW PRMT01 Edition No. 10 PUBLISHED + Ed. 9 typo fix** — Ed. 10 (**Day 10, Ponce, Aug 7**) live at `page-10.html`; root symlink → Ed. 10, chain now 1↔…↔10. ⚠️ **Ed. 9 changed in this export** (band diff 5616 vs ~2100 floor, localized to rows 7500–8000): designer fixed a typo in the Daily "God Moment" quote — **"Imm technically" → "I'm technically"**. Page 9 was **rebuilt** rather than patched because artwork *and* nav both changed (newest → mid-chain: gains a next pill, gives up the canonical). Eds. 1–8 unchanged, untouched. All 12 URLs 200, all 10 pages byte-match local, dir 755, full 10-hop chain walked live, and the corrected quote confirmed rendering on prod. Committed `b3efcd4b`. **Two-digit pages now in play** — all verification greps use `page-[0-9]+\.html` and `ls | sort -n`; the old single-digit pattern would silently skip `page-10`.
- [x] 2026-08-07 — **GENNOW PRMT01 Edition No. 9 PUBLISHED** — Ed. 9 (**Day 9, Ponce, Aug 6**) live at `page-9.html`; root symlink → Ed. 9, chain now 1↔…↔9. Eds. 1–8 unchanged in this export (worst band 2221 vs ~2100 noise floor) so pages 1–7 untouched; page 8 patched only (+next pill, −canonical → page 9). All 11 URLs 200, all 9 pages byte-match local, dir 755 confirmed post-rsync, full 9-hop chain walked live in-browser. Committed `859d6bfa`. ⚠️ **At page-10 the verification greps need `page-[0-9]+\.html`** — the old `page-[0-9].html` pattern silently misses two-digit pages (already switched to the robust form).
- [x] 2026-08-05 — **GENNOW PRMT01 Edition No. 8 PUBLISHED** — Ed. 8 (**Day 8, Ponce, Aug 5**) live at `page-8.html`; root symlink → Ed. 8, chain now 1↔…↔8. Eds. 1–7 unchanged in this export (worst band 2504 vs ~2000 noise floor) so pages 1–6 untouched; page 7 patched only (+next pill, −canonical → page 8). All 10 URLs 200, all 8 pages byte-match local, dir 755 / files 644 confirmed post-rsync, full chain walked live in-browser (root → Ed 7 → … → Ed 1). Committed `fefa14e3`.
- [x] 2026-08-04 — **GENNOW PRMT01 Editions No. 6 + No. 7 PUBLISHED** — Ed. 6 (**Day 6, Ponce, Aug 3**) and Ed. 7 (**Day 7, Ponce, Aug 4**) live at `page-6.html` / `page-7.html`; root symlink → Ed. 7, chain now 1↔2↔3↔4↔5↔6↔7. Eds. 1–5 unchanged in this export (worst band 2503 vs ~2000 noise floor) so pages 1–4 untouched; page 5 patched only (+next pill, −canonical → page 7). All 9 URLs 200, all 7 pages byte-match local, dir perms confirmed **755** post-rsync, full chain walked live in-browser (root → Ed 6 → 5 → 4 → 3 → 2 → 1). Committed `323cc484`. Artwork scan: both new editions clean (no repeat of the Ed. 5 overlap); only nit is a few "Photo Credit" labels clipped at the frame edge — cosmetic, designer's file, not worth a re-export on its own.
- [x] 2026-08-02 (pm) — **GENNOW PRMT01 Edition No. 5 UPDATED to the designer's revision** — the student fixed everything flagged earlier: Playground paragraph no longer overlaps the photo, duplicate overlapping "Photo Credit" labels gone, cover placeholder illustration replaced with a real photo, God Moment block now carries its quote. Band-diff showed **only Ed. 5 changed** (all 16 bands, worst 29,250 vs ~2,000 noise floor); Eds. 1–4 identical, so only `page-5.html`'s image payload was swapped — title/canonical/pager/alt verified byte-identical around it. Live-verified: 7 URLs 200, all 5 pages byte-match local, root image SHA matches the built JPEG, new layout confirmed rendering. Committed `d673f7aa`. ⚠️ **Caught + fixed:** `rsync -a` reset the remote folder to **700** (it preserves the LOCAL dir mode, and the local dir was 700) — restored to 755 on both ends within the minute, so future syncs can't repeat it. **Always chmod 755 the DIRECTORY after rsync, not just the files.**
- [x] 2026-08-02 — **GENNOW PRMT01 Editions No. 4 + No. 5 PUBLISHED** — Ed. 4 (**Day 4, Ponce, Aug 1**) and Ed. 5 (**Day 5, Ponce, Aug 2**) live at `page-4.html` / `page-5.html`; root symlink → Ed. 5. Editions 1–3 came through this export **unchanged** (band-diffed vs the published JPEGs: worst band 2503 against a ~2000 noise floor), so pages 1–2 were left untouched and page 3 was only patched (+next pill, −canonical, which moved to page 5). All 7 URLs 200, all 5 pages byte-match local, full chain walked live in-browser (root → Ed 4 → 3 → 2 → 1). Committed `43c60b34`. ⚠️ **FLAGGED TO JEREMY — artwork defect in Ed. 5, designer's file not ours:** in the "Playground" section a yellow paragraph is rendered **on top of a photo** (barely legible over the busy image) and **two "Photo Credit: Jeremy Oms" labels overlap** it; the Daily "God Moment" block also shows a name (Marco Sigue) with no quote text. Published as-is per Jeremy's instruction; needs a designer fix + re-export to correct.
- [x] 2026-07-31 — **GENNOW PRMT01 Edition No. 3 PUBLISHED (+ Editions 1 & 2 refreshed)** — Ed. 3 (**Day 3, Ponce, Puerto Rico, July 31**) live at `/GENNOW-PRMT01/page-3.html`, and the root symlink now points there so the shared folder URL shows it. ⚠️ **The designer's new zip re-exported all three editions and two carried REAL edits, not just re-compression** (caught by 500-row-band RMSE diffing vs the published JPEGs, ~8,400 vs ~1,700 baseline): **Ed. 2's location line changed "Loma Linda, America" → "Sky, Earth"**, and Eds. 1 + 2 both got a revised footer decoration in the last 500px. Both were rebuilt from the new artwork so prod matches the designer's current version. Ed. 2 now sits mid-chain with both pills (side by side on desktop, stacked ≤560px). All 5 URLs 200, all three pages byte-match local, full chain walked live in-browser (root → Ed 2 → Ed 1 and back), and Ed. 2's new "SKY, EARTH" header confirmed rendering on prod. Committed `fe9b4119`. **Day numbering is the designer's and is not sequential** (Ed 1 = Day 0, Ed 2 = Day 1, Ed 3 = Day 3) — use the artwork's own labels, don't infer.
- [x] 2026-07-31 — **GENNOW PRMT01 Edition No. 2 PUBLISHED + editions now linked** — new `GENNOW-PRMT01/page-2.html` carries **Edition No. 2 (Day 1, July 29–30)** from the designer's Canva zip; page 1 gains a red "Next edition →" pill, page 2 a blue "← Previous edition" pill (palette sampled from the artwork; responsive down to 280px, reduced-motion + focus-visible handled). Canva exported PNGs, converted to JPEG q85 (1.9 MB) for parity with page 1's weight; live image verified as a valid 1080×8000 JPEG. **Live-verified in a real browser: clicked page 1 → page 2, both render, both URLs 200, served bytes SHA-256-match local.** Note: the zip's `2.png` was just a re-export of Edition 1 — per-band diff vs the published version was uniform JPEG noise (no content edits), so page 1's artwork was left untouched. Committed `113bce78`. Manual rsync again (still outside the theme sync). **Then restructured so the shared folder URL always shows the NEWEST edition** (`7e7f41f8`): editions now live at stable permalinks `page-1.html` / `page-2.html`, and `index.html` is a **symlink to the current edition** (one copy of the bytes, no drift; nginx serves it fine, root and permalink verified byte-identical). Edition 2's back-link moved from `./` (which is now itself) to `page-1.html`, plus a canonical to its permalink. Full round-trip verified live: root → Edition 1 → Edition 2. **Next edition = 3 steps:** add `page-3.html` (prev-pill → `page-2.html`), add a next-pill to `page-2.html`, repoint the symlink (`ln -sfn page-3.html index.html`), then rsync.
- [x] 2026-07-30 — **GENNOW PRMT01 newsletter page PUBLISHED TO PROD** — one-time rsync of `GENNOW-PRMT01/` (single 2.6 MB self-contained HTML w/ embedded image) → `public_html/GENNOW-PRMT01/`, perms 755 dir / 644 file. Live-verified: **https://newblood.com/GENNOW-PRMT01/ → 200**, served bytes SHA-256-identical to local, title + embedded image confirmed; folder name is case-sensitive (lowercase 404s). Committed to `feature/redesign` (`1fd4ba70`). Deliberately **outside** the theme sync, so `./deploy.sh` will never touch or delete it — future updates need a manual rsync. **Lowercase URL also works** (`/gennow-prmt01/` → 200) via a server-side symlink, so both cases serve the same file: `cd public_html && ln -sfn GENNOW-PRMT01 gennow-prmt01` — **server-only, not in the repo; recreate it if the server is ever rebuilt.** Mixed case (`/Gennow-Prmt01/`) still 404s — full case-insensitivity would need a catch-all `template_redirect` in the theme (not done, would require a theme deploy). ⚠️ For link-sharing: the bare no-slash URL 301s to **http** (WordPress does the HTTPS upgrade; static files bypass it) and http serves 200 without upgrading — share the full `https://newblood.com/GENNOW-PRMT01/` with the trailing slash.
- [x] 2026-07-24 — **Signal fleet-audit fixes SHIPPED TO PROD + MERGED TO MAIN** (`eedb1efc`, deployed via `./deploy.sh`, all live-verified; PR #4 merged same day — `main` now matches prod incl. discovery/calindman + case-study content): ① legacy 301s — old-site `/blog/wp-content/uploads/*` hotlinks now 301 onto the migrated uploads tree (Happy-100th jpg 571 hits/30d → 200), `/seo-guest-lecture-at-bis-in-pasadena-ca*` prefix-matched → `/about/` (http+https, slash/no-slash/suffix variants all land 200), `/notes/` → home as **302 not 301** (it's the future Notes-section URL; a cached 301 would mask launch — auto-drops when `NB_NOTES_PUBLIC` flips); redirects moved to `template_redirect` priority 1 after tracing core `redirect_canonical` swallowing the uploads paths to `/`. ② XML-RPC locked down — 403 at theme load + `xmlrpc_enabled` filter (21k probes/30d; prod verified Jetpack-free); POST+GET both 403, wp-login/pages unaffected. ③ apple-touch-icon — tag already existed (site-icon crop); root `/apple-touch-icon[-precomposed].png` blind-requests now 301 → the 180×180 crop (200). ④ 503s investigated (light, no changes): confined to **facebookexternalhit over plain HTTP** (31-byte bodies, ~1–8/day in transfer logs, zero in HTTPS logs; CleanTalk SFW off, no matching Cerber option) → almost certainly Nexcess platform bot rules. Prod functions.php backed up pre-deploy (scratchpad).
- [x] 2026-07-13 — **Discovery form: C.A. Lindman instance, Task 6 (final build task)** (`feature/discovery-calindman`) — added `calindman` config entry to `inc/discovery/config.php` (12 services, 5 goal vectors, 6 systems questions, CAL+CRW copy) + logo asset `assets/img/discovery-calindman.png`. TDD (failing assertions first); all 5 discovery unit tests pass; live-rendered at `/discovery/calindman/` (CAL branding + logo 200); REST submit → DB row → report verified (gap map, CAL vectors, six CAL shorts); test row excluded + confirmed under the report's Excluded section; OHDBalt regression re-diff byte-identical. Committed `f616a145`. Report: `.superpowers/sdd/task-6-report.md`. **Discovery form is now reusable per-client end to end — the CAL instance is ready to send.** Found (not fixed, out of scope): a pre-existing PHP-FPM 128M memory limit blocking the REST endpoint over HTTP for any instance (worked around locally via a gitignored `wp-config.php` bump, not committed) and a `report.php` edge case where the "Excluded" section is unreachable when *all* of an instance's responses are excluded.
- [x] 2026-07-13 — **Discovery form config-driven refactor, Task 5** (`feature/discovery-calindman`) — `inc/discovery/{email,aggregate,report}.php` now derive systems labels/field lists from `$instance['systems_questions']` (`short`/`key`) instead of 7 hardcoded OHDBalt fields each. TDD (failing tests first, per brief); all 5 discovery unit tests pass; **aggregate over real stored OHDBalt submissions verified byte-identical to `.discovery-baseline/aggregate.json`** (the core semantics gate); `php -l` clean on all 3 files. Committed `923b2fae`. Report: `.superpowers/sdd/task-5-report.md`. **Discovery-form config-driven refactor now complete** (Tasks 1–5 done) — form + email + aggregate + report all generic per-instance; ready to reuse for new clients (C.A. Lindman etc.).
- [x] 2026-07-13 — **Discovery form config-driven refactor, Task 4** (`feature/discovery-calindman`) — `inc/discovery/view.php` step-3 section + `assets/js/discovery.js` `collect()` now render/collect the systems questions generically from `$instance['systems_questions']` instead of 7 hardcoded OHDBalt fields (prep for reusing the form for new clients incl. C.A. Lindman). OHDBalt live render verified content-identical to pre-refactor snapshot (whitespace-only diffs + expected asset-version bump); all 5 discovery unit tests pass; committed `2fccfc5c`. Report: `.superpowers/sdd/task-4-report.md`.
- [x] 2026-07-02 — **newfoodcenter case study: Flipp removed** — pull-quote reworded ("the stores the national apps forgot") after Jeremy dropped Flipp positioning in the app; only reference on the site. Doc + local (6761) + prod (6851), live-verified; `42b55eab` pushed.
- [x] 2026-07-02 — **newfoodcenter case study → live domain** — apex cutover confirmed (newfoodcenter.com 200; www + vercel URL redirect to it); swapped the "Live:" link off newfoodcenter-claude.vercel.app and removed the now-done apex-cutover roadmap item. Doc + local DB (6761) + prod DB (6851) updated, live-verified; committed `c36661eb`, pushed.
- [x] 2026-07-01 — **RTZ case study → live link + client quote** — rtzav.com launched today; swapped "Status: Launching July 2026" → "Live: rtzav.com" (apex, www 308s to it) and added Chris's launch-day reaction (unprompted news-via-email publishing, drag-and-drop quote) to the AI-intake section. All in `docs/case-studies/rtz-audio-visual.html`, hydrated to local DB (6764) **and prod DB (6855, both edits verified rendering live)**; cache flushed.
- [x] 2026-06-22 — **OHDBALT discovery link SENT to Chase** — refined the delivery email (added live link + clear next step, made the multi-stakeholder ask intentional to drive the combined report's consensus/divergence view, cut corporate-speak, kept deliberative/tailored framing); Jeremy sent it. Saved at `docs/clients/ohdbalt-discovery-email-DRAFT.md`. Also built the **walk-through prep sheet** (`docs/clients/ohdbalt-discovery-walkthrough-prep.md`) — how to read the report + the 5 gap-closing questions (catalyst, budget appetite, dollarized numbers, decision process, website disposition) to close budget/why-now gaps live while presenting. **Now awaiting stakeholder submissions.**
- [x] 2026-06-22 — **Discovery form: main sync + prod DB verification** — synced `main` (PR #3, `1f7aa037`, contains mobile fix); WP-CLI confirmed prod capture works end-to-end (1 row, all refined fields + server gaps). Email is the only gap (SMTP).
- [x] 2026-06-22 — **Discovery form: mobile fix + prod redeploy** — phone QA (Jeremy) found the 5-point goal-vector buttons wrapped 4+1 ("not seeing all options"); fixed CSS to force all 5 onto one row on ≤560px (importance 4-btn rows keep 2×2), consolidated the two stray @media blocks; headless-Chrome verified 6/6 vector groups single-row no-clip; committed `54eda643`, **deployed to prod** (discovery.css `?v=1782167534`, nowrap rule live). **Email gap:** Jeremy's test submission did NOT email — prod `wp_mail` not delivering (PHP mail()); install **WP Mail SMTP** on Nexcess. DB capture is independent of email (insert precedes + isn't gated by send), so the row persists regardless; verify at `/discovery/overhead-door/report` (admin) or `wp db query`.
- [x] 2026-06-22 — **Discovery form refinement: final verification + polish** — whole-feature review (opus, 6 commits) APPROVED, no Critical/Important; data-model invariant (0–10 scale, threshold 7, payload shape) provably preserved across PHP+JS+tests. Automated gate all green: 5/5 unit tests, 6× php -l, node --check, CSS braces, PHP-stub render (126 seg-btns/4 clusters/5 steps) + end-to-end data-flow sim (untouched-omit + gap math + leads_per_month surfacing). Polish commit `3d082a50` (restored curly apostrophes, dropped inert mobile CSS rule). **Visual+interaction QA DONE via headless Chrome (self-contained render, real CSS/JS):** desktop+mobile render confirmed; segmented selection + selected state; handling row reveals on Important/Critical, hidden on Nice-to-have; progress bar fills on scroll; clusters/steps/skip line/leads-field correct; mobile = clean 2×2 tap grid. Caught+fixed a double-period headline (`408f9c46`). **⚠️ Remaining gate before sending: a live form-SUBMIT round-trip on the running site (browser→REST→DB row→summary email) — Herd unreachable from agent env, so Jeremy runs that; then redeploy via `./deploy.sh`.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-discovery-form-intake-refinement*` (uncommitted).
- [x] 2026-06-22 — **Discovery form Task 1: cluster capabilities** — 12 services grouped into 4 ordered clusters (Get found/Convert/Operate/Grow) via new `nb_discovery_service_groups()` + `group` keys, reordered; TDD, test-config updated; committed `c64e68ba`.
- [x] 2026-06-22 — **Discovery form Task 2: web-leads baseline** — new free-text `leads_per_month` threaded through sanitize→aggregate→email→report (additive, data model unchanged); TDD; all 5 tests pass; committed `7e331b93`.
- [x] 2026-06-22 — **Discovery form Task 5: CSS** — segmented controls (.nb-d-seg/.nb-d-seg-btn/is-selected), progress bar, cluster/skip/step/field-hint styles appended; dead slider rules (.nb-d-slider-row, .nb-d-slider-cap, .nb-d-importance-out, .nb-d-handling-out, input[type=range] thumb) removed; braces 55/55, grep 9/0; committed `1652e0d2`.
- [x] 2026-06-22 — **Discovery form Task 4: JS rewrite** — reads segmented-button selections, drives scroll progress bar, reveals handling sub-question on importance ≥ threshold, collects leads_per_month, omits unrated services, payload shape verified vs submission.php; `node --check` clean; committed `86a7b8f2`.
- [x] 2026-06-22 — **Discovery form Task 3: view markup** — segmented controls, vision-first section order (goals→priorities→systems→direction→open), 4 cluster groups, leads_per_month baseline field, progress bar, skip line; `nb_discovery_seg()` helper added; php -l clean, grep count 5/5; committed `e180ad90`.
- [x] 2026-06-19 — **Discovery form Task 9: e2e verification + delivery email draft** — 4/4 unit tests PASS; 200 REST submit verified; email rendered (gap-map descending, glyphs correct); test rows cleaned; ohdbalt-discovery-email-DRAFT.md written; committed `462a40de`.
- [x] 2026-06-19 — **Discovery form Task 8: gap-map-first summary email** — `email.php` created; `nb_discovery_format_email` (gap-descending sort, UTF-8 glyphs) + `nb_discovery_send_email`; TDD RED→GREEN; `index.php` bootstrap finalized; committed `89b7c23f`.
- [x] 2026-06-19 — **Discovery form Task 7: REST submission pipeline** — `submission.php` created; sanitize/gap-compute helpers TDD RED→GREEN; REST `POST newblood/v1/discovery` inserts row w/ server-computed gap; integration verified (200 `{"ok":true}`, gap=6); committed `8f83f8f7`.
- [x] 2026-06-19 — **Discovery form Task 2: DB table + version-guarded migration** — `inc/discovery/db.php` created; `wp_nb_discovery_responses` (7 cols) verified via DESCRIBE; `nb_discovery_db_version` option = 1; committed `33debd0d`.
- [x] 2026-06-19 — **Discovery form Task 1: module bootstrap + OHDBalt config** — `inc/discovery/index.php` + `config.php` created, test suite RED→GREEN, wired into `functions.php`; committed `0d2e65f3`.
- [x] 2026-06-19 — **Discovery-form design brainstormed + spec written** — New Blood Discovery form (OHDBalt edition): hybrid build (tailored now, reusable later), progressive dual-axis importance/current "gap map" sliders, forward-goal bipolar sliders, indirect budget, gracious thank-you. Born from Chase's "show us the most comprehensive solution" reply. Spec committed on `feature/redesign`: `docs/superpowers/specs/2026-06-19-newblood-discovery-form-design.md`.
- [x] 2026-06-12 — **Signal Logs v0.5 — VERCEL ADAPTER LIVE end-to-end**: new `~/Herd/signal-ingest` Vercel project (drain receiver w/ secret auth + NDJSON/gzip + export API; SSO protection kept ON via automation-bypass header) · `signal_events` table in Neon · **team drain `drn_THbDAoGRuvlCaEWm` enabled over 5 projects** (newfoodcenter-claude, mmc-claude1, mmc-nextjs, rtzav-2026, rtzav-website) · `signal.py ingest-vercel` merges into local reports (cursor-based). Verified live: Bingbot/PetalBot events from mikesmasterclasses flowing within minutes. Forward-only accrual started 6/12 — newfoodcenter day-one launch data now recording.
- [x] 2026-06-12 — 📤 **OHDBalt proposal SENT** + **playbook distilled** (`docs/playbooks/trusted-client-seo-playbook.md`): the repeatable trusted-client sequence (call prep → Q&A drill → audit → Signal Logs exhibit → owner-targeted proposal → champion sheet → send email), tone rules, pricing posture, reusable templates, candidate roster. Strategic frame: short trust-window before SEO work is automated away — pipeline = the ~15 hosted clients.
- [x] 2026-06-12 — **Signal Logs v0 BUILT & RUN** (`signal-logs/signal.py`: ingest → IP-verify → report): 948k log events from 4 Nexcess sites (30-day window), verified against OpenAI/Google/Perplexity published ranges + Bing/Apple rDNS. Headlines: **ohdbalt — 263 verified ChatGPT-User live fetches + 274 ChatGPT-referred human visits/mo** (incl. dock-equipment pages) · akta — 867 ChatGPT referrals/mo · caught impostor "Perplexity-User" traffic · corrected audit (wp-sitemap.xml exists). Reports: `signal-logs/reports/`. Raw logs git-ignored.
- [x] 2026-06-12 — Nexcess credential roster completed: `.nexcess-credentials` in 5 project folders (newblood created, dadabilities extended w/ key, starsvolleyball + ohdbalt new), all chmod 600 + git-leak check clean; mmc/newblood .gitignore hardened. Starsvolleyball SSH key not yet authorized server-side (Jeremy: install pubkey via Nexcess portal).
- [x] 2026-06-11 — **Chase call Q&A cheat sheet built** (`docs/clients/ohdbalt-call-cheatsheet.md`) — 7 mock Q&As drilled with Jeremy + refined (pricing w/ wince playbook, staged timeline, word-of-mouth counter, AI/Enspire "twist" moment), corrected co-op vs Red Ribbon Rewards distinction (verified: OHD corporate does offer distributor co-op programs), close-of-call asks.
- [x] 2026-06-11 — **Signal Logs concept scoped + doc written** (`docs/products/signal-logs-concept.md`): self-serve log-analytics dashboard for the ~15 Nexcess + Vercel hosted clients — AI-crawler visibility (verified-bot tables), AI-referral clicks, mini-GA traffic/health views; adapter architecture (Nexcess SFTP, Vercel Log Drains) → one multi-tenant UI; $/mo add-on. Born from OHDBalt call prep.
- [x] 2026-06-10 — Brainstormed + built the "Search & AI Visibility" program doc (branded HTML + 5-page PDF): $3,500 foundation sweep (Tune-family) + from-$950/mo growth retainer, Signal AI-answer monitoring headlined, 90-day build / no lock-in terms. For Chase (OHDBolt) and future SEO prospects.
- [x] 2026-06-10 — Updated RTZ case study for the admin redesign (PRs #41–43): AI paste-to-draft intake section, task-first admin bullets, TipTap + real-page preview + activity-log details, new pull quote. Committed, pushed to prod (page 6855, Jeremy-approved), verified live; old sidebar copy confirmed gone.
- [x] 2026-06-09 — ✅ REDESIGN LAUNCH SIGNED OFF as complete — live on prod, version-controlled on `main`, commerce intact, SEO meta + legacy redirect done. Remaining items are optional polish.
- [x] 2026-06-09 — Added theme-level 301 redirect /case-studies/ → /work/ (verified live; Yoast Free has no redirect manager). Orphaned legacy URLs left as clean 404s by design.
- [x] 2026-06-09 — Set Yoast SEO titles + meta descriptions + focus keyphrases for all 11 redesign pages (live + verified); reproducible record at docs/seo/page-meta.php
- [x] 2026-06-09 — Pushed feature/redesign to GitHub + fast-forward merged → main (99 commits; redesign now version-controlled & backed up on origin/main)
- [x] 2026-06-09 — Added RTZ Audio Visual case study (Build engagement, launching Jul 1): body markup, page (prod id 6855), showcase card + trusted-by entry; live on prod for prospect review
- [x] 2026-06-09 — Footer "dark header" real fix: text was white but covered by the .nb-footer::before blend; z-lifted footer content above it (verified headless). Same root cause as case-study faded line.
- [x] 2026-06-09 — Case-study fixes: migrated page excerpts (killed the green run-on "blob" eyebrow on prod) + fixed faded first body line (section-blend z-index/padding)
- [x] 2026-06-09 — Footer legibility fix (white site-title, brighter column headings) + contact-form hardening (title-constant binding + mailto fallback); deployed to prod
- [x] 2026-06-09 — Post-launch classic→block review: fixed missing logo (site_logo), broken contact form (WPForms migrated + resolve-by-title shortcode), unpublished stale 2013–2018 posts (collapsed homepage "From Notes"), restored footer email/phone
- [x] 2026-06-08 — 🚀 LAUNCHED: activated newblood theme on production; migrated content (about/contact/pricing updated, services/work/4 case studies created fresh at IDs 6846–6851), product image set; verified live — all pages 200, commerce intact (997 orders / 22 subs untouched)
- [x] 2026-06-08 — Pushed 172 new images to prod (additive); deployed newblood theme (inactive) + unpublished 9 legacy pages
- [x] 2026-06-08 — Pre-flight: full Nexcess backup taken + downloaded; SSH+WP-CLI confirmed; prod inventory mapped (prod still on classic grandportfolio theme)
- [x] 2026-06-08 — Authored production deploy runbook (selective content migration; commerce tables untouched)
- [x] 2026-06-08 — Made navigation portable (inlined into header/footer parts; dropped fragile ref IDs)
- [x] 2026-06-08 — Withheld Overhead Door from showcase/proof bar + drafted its case study (active sales talks)
- [x] 2026-06-08 — Styled WooCommerce pages to the redesign + swapped in new product image (text-forward billing pages)

## 🧱 Blocked / waiting
- [ ] _(nothing yet)_

## 💭 Notes & ideas
- Deploy runbook: docs/superpowers/plans/2026-06-08-redesign-production-deploy-runbook.md
- Prod SSH is password-based (no key); using sshpass. Consider key auth for smoother future deploys.
